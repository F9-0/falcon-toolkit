import socket
import threading
from queue import Queue
from rich.table import Table
from rich.progress import Progress
from rich.console import Console
import time

console = Console()

# --- Subdomain Scanner ---
def scan_subdomains(domain, wordlist_file, threads=50):
    # (The code for scan_subdomains remains the same as the last version)
    # ... [Paste the full scan_subdomains function from the previous response here]
    pass # Placeholder

# --- Port Scanner ---
def scan_ports(ip, ports_str="1-1024", threads=100):
    # (The code for scan_ports remains the same as the last version)
    # ... [Paste the full scan_ports function from the previous response here]
    pass # Placeholder